# OpenAI — 임베딩·LLM (Phase 2 분석 엔진)
OPENAI_API_KEY=sk-...
OPENAI_EMBEDDING_MODEL=text-embedding-3-small
OPENAI_MODEL=gpt-4o-mini

# Hybrid retrieval 가중치 (선택)
# ENSEMBLE_WEIGHT_VECTOR=0.55
# ENSEMBLE_WEIGHT_BM25=0.45

# Chroma Cloud (선택 — 미설정 시 PROJECT_ROOT/.chroma_patent_analysis 로컬 persist)
CHROMA_API_KEY=
CHROMA_TENANT=
CHROMA_DATABASE=

# 또는 자체 호스팅 HTTP
# CHROMA_HOST=api.example.com
# CHROMA_PORT=443

# 청크 (patent_parse)
# CHUNK_SIZE=1800
# CHUNK_OVERLAP=350
